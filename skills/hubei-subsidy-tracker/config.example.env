# 可选协作平台配置模板；本地模式无需飞书凭证
# 新安装可复制为 config.env；已有配置仅补缺项，不覆盖。独立路径使用 SUBSIDY_CONFIG_FILE；
# config.env 仅存本地，不进记忆/仓库/版本库。
# 运行模式三选一：local / feishu / dingtalk
SKILL_MODE=local
#
# 获取方式：
#   1. 飞书开放平台(https://open.feishu.cn) → 创建"企业自建应用" → 拿到 app_id / app_secret
#   2. 给应用开通「多维表格」读写权限
#   3. 在目标多维表格内：右上角"..." → 更多 → 添加应用，把自建应用加为协作者
#   4. 多维表格里建三张数据表：政策清单 / 企业档案 / 匹配结果（字段见 references/fields.md）
#   5. （可选）飞书群 → 设置 → 群机器人 → 自定义机器人 → 拿到 Webhook 地址
#
# 填完后运行 python scripts/feishu_sync.py --check 自检。

FEISHU_APP_ID=cli_xxxxxxxxxxxxxxxx
FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 多维表格位置：直接粘贴完整 URL 即可（脚本自动取 base/xxxx 段）
FEISHU_BASE_URL=https://xxxx.feishu.cn/base/bascnxxxxxxxxxxxxxxxx

# （可选）三张数据表的 table_id。不填则按表名自动定位
# （政策清单→含"政策"，企业档案→含"企业"，匹配结果→含"匹配"）
# FEISHU_POLICY_TABLE=tblxxxxxxxxxxxxxx
# FEISHU_ENT_TABLE=tblxxxxxxxxxxxxxx
# FEISHU_MATCH_TABLE=tblxxxxxxxxxxxxxx

# （可选）群机器人 Webhook，用于推送每日播报/匹配结果到飞书群
# FEISHU_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx-xxxx

# （可选）企业资料知识库：粘贴知识库任意页面 URL 即可
# 也可运行 python scripts/feishu_wiki.py --init 由应用创建
# FEISHU_WIKI_URL=https://xxxx.feishu.cn/wiki/xxxxxxxxxxxxxxxx

# 钉钉群级通知（可选）。适配器默认 dry-run，不会真实外发；只有显式使用 --send 才发送。
# 路由文件放在 data/ 下（已被 .gitignore 忽略），按企业档案“通知人”精确映射到群 Webhook。
# 可复制 references/dingtalk_routes.example.json；禁止 default 群兜底。
# DINGTALK_ROUTE_FILE=dingtalk_routes.json
# DINGTALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=REPLACE_ME
# DINGTALK_SECRET=REPLACE_ME
# 上述变量只支持通用群级 Webhook。
# 千问办公中的钉钉 AI 表格和个人单聊使用 data/dingtalk_config.json、
# data/dingtalk_notify_users.json 及 scripts/dingtalk_aitable_sync.py；
# 配置流程见 references/dingtalk_qwen.md，真实 ID 不得写回本模板。
